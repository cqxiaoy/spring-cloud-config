server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yanxiao520/spring-cloud-config.git
          searchPaths: config
          
  application:
    name: config-server

test: hello application.yml
yx:
  name: yx
  age: 22
  sex: 男